import type { RouterProps } from 'ripple-router/types';

import { RoutesContext } from 'ripple-router/contexts/RoutesContext.ripple';
import { Renderer } from 'ripple-router/components/Renderer.ripple';

export component Router(props: RouterProps) {
  RoutesContext.set(props.routes);

  <Renderer />
}
