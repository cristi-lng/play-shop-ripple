import { CartProvider } from 'src/store/cartContext.ripple';
import { RouterProvider } from 'src/router/routerProvider.ripple';
import 'src/styles/global.css';

component App() {
  <CartProvider>
    <RouterProvider />
  </CartProvider>
}

export { App };
