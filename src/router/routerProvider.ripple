import { Router } from 'ripple-router';

import { ProductsPage, ProductDescriptionPage, ProductReviewsPage } from 'src/router/routes.ripple';

component RouterProvider() {
  <Router
    routes={[
      { path: '/', element: ProductsPage },
      { path: '/:productId', element: ProductDescriptionPage },
      { path: '/:productId/reviews', element: ProductReviewsPage },
    ]}
  />
}

export { RouterProvider };
